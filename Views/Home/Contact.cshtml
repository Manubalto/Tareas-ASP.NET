@{
    ViewBag.Title = "Contact";
}
<div class="container mt-5">
    <h2 class="mb-4 text-center">Listado de Tareas</h2>
    <h3 class="text-center">A continuación se muestra la lista de tareas registradas por el usuario:</h3>
    <h4>Estas son las tareas ordenadas por Fecha:</h4>
    <h5>Si quieres agregar una nueva tarea, puedes hacerlo desde el <a href="formulario.php">Formulario de Tareas</a>.</h5>
    <h6>Si deseas volver al inicio, puedes hacerlo desde <a href="WebEmmanuel.html">aquí</a>.</h6>

    <?php if ($tareas && count($tareas) > 0): ?>
    <table class="table table-striped table-bordered">
        <thead class="table-dark">
            <tr>
                <th>ID</th>
                <th>Tarea</th>
                <th>Fecha</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($tareas as $tarea): ?>
            <tr>
                <td><?= htmlspecialchars($tarea['idTareas']) ?></td>
                <td><?= htmlspecialchars($tarea['tarea']) ?></td>
                <td><?= htmlspecialchars($tarea['fecha']) ?></td>
            </tr>
            <?php endforeach; ?>
        </tbody>
    </table>
    <?php else: ?>
    <div class="alert alert-info">No hay tareas registradas.</div>
    <?php endif; ?>
</div>